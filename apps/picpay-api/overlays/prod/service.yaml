apiVersion: v1
kind: Service
metadata:
  name: picpay-api-prod
  labels:
    app: picpay-api
    environment: production
spec:
  selector:
    app: picpay-api
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30000
  type: NodePort